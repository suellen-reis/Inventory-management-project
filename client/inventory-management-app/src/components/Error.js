import React from "react";

const Error = () => {
  return (
    <div
      style={{
        color: "red",
        padding: "10px",
        border: "1px solid red",
        borderRadius: "5px",
        marginBottom: "10px",
      }}
    >
      <p>Page not found.</p>
    </div>
  );
};

export default Error;
